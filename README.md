# MoneyForward ME 資産トラッカー

マネーフォワード ME の「資産」画面から1日1回データを取得し、資産の推移・分析・将来予測を行う個人用ツール。

## 目的

- 毎日の資産状況をスナップショットとして記録する
- 口座別・銘柄別・資産クラス別の増減を把握する（前日/前月/前年比較）
- 保有株式の業種分散と配当予測を確認する
- モンテカルロ・シミュレーションで将来の資産レンジを推定する

## 全体の流れ

```
[ Playwright (ブラウザ自動操作) ]
    ↓  HTML / Screenshot 保存
[ raw/ ディレクトリ ]
    ↓  パース
[ SQLite DB ]
    ↓  分析・予測
[ Web ダッシュボード / CLI ]
```

サイトへのアクセスは**1日1回のみ**。取得した raw データを元にすべての分析を行い、再アクセスはしない。

## セットアップ

```bash
# 仮想環境の作成と依存インストール
uv venv .venv
source .venv/bin/activate
uv pip install -e .

# Playwright のブラウザをインストール
playwright install chromium
```

> システムに Chromium の依存ライブラリが足りない場合:
> `sudo playwright install-deps chromium`

## 使い方

### 1. 初回ログイン（手動）

ブラウザが開くので、マネーフォワードに手動でログインする。
ログイン完了後、ターミナルで Enter を押すとセッション情報が `.auth/storage_state.json` に保存される。

```bash
python -m src.scraper.login
```

### 2. 日次データ取得（毎日1回）

取得 → パース → DB保存 を1コマンドで実行する。
前回からデータに変化がない場合は自動的に**一括更新をリクエスト**し、待機後に再取得する。

```bash
python -m src.daily                # デフォルト（更新待ち60秒）
python -m src.daily --wait 30      # 更新待ちを30秒に変更
python -m src.daily --no-refresh   # 一括更新を行わない（従来動作）
```

raw データは `raw/YYYY-MM-DD_HHMMSS/` に保存され、解析結果が `data/assets.db` に格納される。

### 3. 配当データ更新

Yahoo Finance Japan から各銘柄の年間予想配当（1株配当）を自動取得し `data/dividends.json` に保存する。
ETF など配当情報が取得できない銘柄は 0 円として記録される。

```bash
python -m src.data.dividend_fetcher           # 全銘柄を取得
python -m src.data.dividend_fetcher 5401 9433  # 指定銘柄のみ
```

### 4. ダッシュボード表示

```bash
python -m src.web.server
# → http://localhost:8080
```

**起動時の自動更新:** 当日の資産データや配当データがなければ、サーバー起動前に自動で取得を実行する。
手動で事前に `src.daily` や `src.data.dividend_fetcher` を実行する必要はない。

```bash
python -m src.web.server               # 自動更新あり（推奨）
python -m src.web.server --skip-update  # 自動更新をスキップ
```

ダッシュボードの機能:
- 資産総額と資産クラス別内訳（円グラフ）
- 前日比 / 前月比 / 前年比の変動表示
- 口座一覧（残高順）
- 保有銘柄一覧（クラス別グループ、各比較期間の差分付き）
- 株式の業種別内訳（円グラフ）
- 年間配当予測（銘柄別内訳つき）
- ライフプランページへのリンク
- 日付セレクタで過去データの閲覧

### 5. ライフプランニング

ダッシュボード右上の「ライフプラン →」からアクセスできる。

ページ構成:
- **月次資産推移** — スナップショットから月末総資産の折れ線グラフ + テーブル
- **月次収支** — 月次の収入・支出・収支テーブル + 棒グラフ（支出には積立投資を含む旨の注記付き）
- **成長予測（追加投資なし）** — モンテカルロ・シミュレーション（P10/P50/P90）
- **成長予測（積立込み）** — 月額積立額を設定して再計算可能（設定は DB に保存）

積立額は `/plan?contrib=80000` のように変更すると DB に永続化され、次回以降もその値が使われる。

### 6. CLI レポート

```bash
python -m src.cli.report
```

### 7. デモモード（ダミーデータ）

実データを使わず架空のポートフォリオで表示する。SNS共有などに。

```bash
python -m src.web.server --demo
```

## プロジェクト構成

```
money_forward/
├── pyproject.toml
├── .gitignore
├── raw/                            # raw データ（git管理外）
├── data/                           # SQLite DB・配当JSON（git管理外）
├── .auth/                          # セッション情報（git管理外）
├── src/
│   ├── scraper/
│   │   ├── login.py                # 手動ログイン → storageState 保存
│   │   └── fetch.py                # Playwright でページ取得・raw 保存
│   ├── parser/
│   │   ├── normalize.py            # 資産 HTML → 構造化データ
│   │   └── cashflow.py             # 月次収支 HTML → 収入・支出データ
│   ├── db/
│   │   ├── schema.py               # SQLite スキーマ定義・初期化
│   │   └── repository.py           # スナップショット・月次収支・設定の保存・取得
│   ├── analysis/
│   │   ├── compare.py              # 前日/前月/前年比較
│   │   └── metrics.py              # ボラティリティ・ドローダウン等
│   ├── prediction/
│   │   └── montecarlo.py           # モンテカルロ成長予測
│   ├── data/
│   │   ├── stock_master.py         # 銘柄マスタ（業種・配当）
│   │   └── dividend_fetcher.py     # Yahoo Finance から配当自動取得
│   ├── cli/
│   │   └── report.py               # CLI レポート
│   ├── web/
│   │   └── server.py               # Web ダッシュボード
│   └── daily.py                    # 日次パイプライン
└── tests/
```

## 技術スタック

| 項目 | 技術 |
|------|------|
| ブラウザ自動操作 | Playwright (Chromium, headless=False) |
| HTML パース | BeautifulSoup4 + lxml |
| データベース | SQLite (WAL モード) |
| Web サーバー | Python 標準ライブラリ (http.server) |
| フロントエンド | vanilla HTML/CSS/JS, Canvas 円グラフ |
| 配当データ取得 | Yahoo Finance Japan (urllib.request) |
| 成長予測 | モンテカルロ・シミュレーション (stdlib random) |
| パッケージ管理 | uv |

外部フレームワーク不使用。Web ダッシュボードは stdlib の HTTPServer で動作する。

## 成長予測について

モンテカルロ・シミュレーションで 1年/3年/5年 後の資産額を P10（悲観）/ P50（中央）/ P90（楽観）のレンジで推定する。

- **リスク資産**（株式・投信）のみ市場変動の対象としてシミュレーション
- **安全資産**（預金・不動産・年金）は変動なしで固定加算
- 幾何ブラウン運動（対数正規モデル）で月次リターンを生成、10,000回実行
- パラメータは過去の日次リターンから推定（データ5日未満の場合はデフォルト: リターン5%/年、ボラ15%/年）
- 「積立込み」モードでは月額積立額を指定可能

ダッシュボードの `?` ボタンから詳細な説明を確認できる。

## ロードマップ: 資産形成アドバンスコース機能の実装状況

[マネーフォワード ME 資産形成アドバンスコース](https://moneyforward.com/pages/extra_premium)の機能を Web で再現することを目指している。

### 実装済み

| 機能 | 状態 | 備考 |
|------|------|------|
| 資産クラス別内訳（円グラフ） | 実装済み | 5クラス分類 |
| 株式の業種別内訳（円グラフ） | 実装済み | 国内株のみ |
| 年間配当予測（銘柄別内訳） | 実装済み | Yahoo Finance から DPS 自動取得 |
| 前日比 / 前月比 / 前年比 | 実装済み | 総額・クラス別・銘柄別 |
| モンテカルロ成長予測 | 実装済み | `/plan` に配置。アドバンスにはない独自機能 |
| 積立シミュレーション | 実装済み | `/plan` に配置。月額は DB 設定に永続化 |
| ライフプランニングページ | 実装済み | `/plan` — 月次資産推移・月次収支・成長予測を統合 |
| 月次収支スクレイピング | 実装済み | `/cf/monthly` から収入・支出を取得しDB保存 |
| ボラティリティ・ドローダウン | 実装済み | metrics.py に実装（ダッシュボード未連携） |

### 未実装

| 機能 | 優先度 | 内容 |
|------|--------|------|
| 配当利回り表示 | 高 | 銘柄ごとの配当利回り（現在値ベース）と取得利回り（取得単価ベース）を配当テーブルに追加。HTML に「平均取得単価」「現在値」が含まれているがパーサが読み飛ばしている（`cells[3]`, `cells[4]`）ため、まず normalize.py で取得単価・現在値をパースし DB に保存する |
| 配当利回り別内訳 | 中 | 利回りレンジ別（0-2%/2-4%/4%超 等）の資産内訳を円グラフで表示。配当利回り表示の実装が前提 |
| 業種別配当内訳 | 中 | 業種ごとの年間配当金額・配当利回りの一覧と円グラフ。現在の業種別内訳（評価額ベース）に配当軸の集計を追加する |
| 資産タグ付け機能 | 非対応 | 本家アプリ独自の UI 機能であり、Web ダッシュボードでの実装メリットが薄い |
| カスタムポートフォリオ | 非対応 | タグ機能が前提。必要な切り口（リスク/安全、国内/海外等）は既存のクラス別・業種別で概ねカバーしている |
| 米国株の業種別内訳 | 低 | 現在は国内株のみ対応。米国株の銘柄コード体系（ティッカー）に対応した業種マスタと配当取得が必要 |
| 配当履歴（月別・年別） | 最低 | 過去に受け取った配当金の実績表示。スナップショットでは配当入金を個別に検出できず、取引履歴スクレイピングか手動登録が必要なため実装コストが高い。証券口座側で確認可能な情報のため優先度を下げる |

### 改善が必要な既存機能

| 機能 | 問題 | 対応方針 |
|------|------|----------|
| 配当利回り計算（server.py） | `div_yield` を計算しているが数式が壊れており、算出結果をどこにも表示していない | 正しい利回り計算に修正し、配当テーブルに利回り列を追加する |
| 株式パーサの取得漏れ | normalize.py が株式テーブルの「平均取得単価」「現在値」「評価損益」「評価損益率」を読み飛ばしている | `cells[3]`〜`cells[8]` もパースし HoldingData / DB に保存する。配当利回り・損益表示の前提 |
| 成長予測のリターン設定 | 全リスク資産（国内株・投信）を同一リターン/ボラで計算しており非現実的。また年金（iDeCo=先進国株式、企業型DC）が安全資産扱いで成長ゼロになっている | 資産クラスをさらに細分化し、クラスごとに異なるリターン/ボラを設定する。例: 国内株（リターン7%/ボラ20%）、先進国株投信（リターン8%/ボラ18%）、株式型年金（リターン8%/ボラ18%）、預金（0%）、不動産（1%）など。年金は中身に応じてリスク資産側に分類し直す |
| ボラティリティ等の指標 | metrics.py に実装済みだがダッシュボードに未連携 | ダッシュボードにリスク指標セクションを追加する |
| 業種マスタの拡張性 | stock_master.py にハードコードされた13銘柄のみ対応。新規銘柄追加時に手動編集が必要 | Yahoo Finance 等から業種情報も自動取得する仕組みを検討する |

## 注意事項

- 本ツールは個人利用・自己分析用途に限る
- サイトへのアクセスは1日1回を厳守し、サーバーへの負荷を最小化する
- `.auth/`（セッション情報）、`raw/`（生データ）、`data/`（DB）は git 管理外
